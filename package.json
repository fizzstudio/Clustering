{
  "name": "@fizz/clustering",
  "private": false,
  "version": "0.12.0",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": "./dist/clustering.js",
  "types": "./dist/clustering.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && api-extractor run --local --verbose",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "polygon-clipping": "^0.15.7",
    "robust-point-in-polygon": "^1.0.3"
  }
}
